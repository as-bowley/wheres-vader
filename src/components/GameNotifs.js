import React from "react";

const GameNotifs = ({ gameNotification }) => {
  return (
    <div className="gameNotifs">
      <h4>{gameNotification}</h4>
    </div>
  );
};

export default GameNotifs;
